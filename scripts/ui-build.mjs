#!/usr/bin/env node
// Placeholder for the shared ui/ build (#6). Kept so the root npm-scripts
// interface is complete; succeeds as a no-op until ui/ has real sources.
console.log("ui/: no build step yet (arrives with #6) — nothing to do.");
