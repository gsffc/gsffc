# site/ — www.gsffc.org (Eleventy)

```bash
npm ci && npm run serve   # live-reload preview on http://localhost:8080
npm run build             # full build (zh + /en/) into _site/
```

Node 22 (`.nvmrc`). No other system dependencies. Canonical contributor doc:
[../AGENTS.md](../AGENTS.md); procedures: [../docs/runbooks/](../docs/runbooks/).
