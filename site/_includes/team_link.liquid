{% if team %}
<img class="team-logo" src="{{ team.logo | assetImg }}" />
<a href="{{ "/seasons/" | append: seasonKey | append: "/" | append: team.key | append: "/" | langUrl }}">{{ team | dname }}</a>
{% else %}
&nbsp;
{% endif %}
