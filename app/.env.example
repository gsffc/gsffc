DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME
SESSION_SECRET=change-me
# Zone the events' wall-clock start/end times are read in — the process itself
# runs in UTC on Netlify. Only the check-in window depends on it. Default:
CLUB_TIMEZONE=America/Los_Angeles